# RideSync Admin App

Admin panel entrypoints live in `pages/admin_*.php` while the business rules move into the backend service layer. New admin routes should be thin controllers over `backend/services`.
