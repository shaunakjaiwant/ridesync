# AI Domain Boundary

AI verification runtime code lives in `apps/ai-verification`. Shared domain notes, model assets, provider playbooks, and future pipeline modules belong under this top-level AI boundary.
