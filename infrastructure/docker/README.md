# Docker Infrastructure

Container composition remains in the root Compose files for local ergonomics. Service-specific Docker assets should be placed here when they are not owned by an individual app.
